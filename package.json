{
  "name": "blockchain-certificate-dapp",
  "version": "1.0.0",
  "description": "Decentralized application for issuing and verifying certificates on blockchain",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "test": "hardhat test",
    "node": "hardhat node",
    "frontend": "cd frontend && npm run dev"
  },
  "keywords": [
    "blockchain",
    "certificate",
    "ethereum",
    "ipfs",
    "dapp"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^17.2.3"
  }
}
